import { FC } from "react";
import { NotFoundComponent } from "../../features";

export const NotFoundPage: FC<{}> = (): JSX.Element => {
  return (
    <>
      <NotFoundComponent />
    </>
  );
};
